apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gpt-clawunit-playwright-browsers
  namespace: gpt-clawunit
  labels:
    app: clawunit
    component: playwright-browsers
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
